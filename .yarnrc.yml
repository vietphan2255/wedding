# Use a classic node_modules install instead of Yarn Plug'n'Play.
# PnP breaks vite/esbuild resolution of transitive deps (e.g. @firebase/app)
# and the TS SDK isn't wired up here. node-modules keeps `yarn` and `npm`
# interchangeable and is the linker this project expects.
nodeLinker: node-modules
