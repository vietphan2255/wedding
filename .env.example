# Copy this file to .env.local and fill in real values from your Firebase project
# Project settings → Your apps → Web app → SDK setup and configuration
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxx
# Google Analytics (GA4). Optional — omit to disable analytics. Firebase Console
# → Project settings → Your apps → Web app → measurementId.
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Cloudinary — free image & audio uploads from the admin (no credit card needed).
# cloudinary.com → Settings → Upload → add an UNSIGNED upload preset.
# Set the preset's "Resource type" to Auto so background-music audio uploads too.
# Cloud name is shown at the top of the dashboard.
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=your-unsigned-preset

# Admin allowlist — comma-separated emails permitted into /admin. Sign-in uses
# Firebase Authentication (Google or email/password); only accounts whose email
# appears here may enter, and the SAME emails MUST be mirrored in
# database.rules.json (the real boundary — this var is just client-side UX /
# early-rejection). New accounts gain nothing: a non-listed account that signs in
# lands on a "Not authorized" screen and the rules grant it no data access. There
# is no VITE_ADMIN_PASSWORD anymore — a VITE_ var ships in the public bundle and
# could never be a real secret.
VITE_ADMIN_EMAILS=admin@example.com,second-admin@example.com
